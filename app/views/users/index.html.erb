<% provide(:title, 'All users')%>
<p id="notice"><%= notice %></p>

<h1>All users</h1>

<%= will_paginate %>

<ul class="user-list">
    <%= render @users %>
</ul>

<%= will_paginate %>

<br>

<%= link_to 'New User', new_user_path %>
